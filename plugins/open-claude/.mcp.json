{
  "mcpServers": {
    "open-claude": {
      "command": "bash",
      "args": ["-c", "OPEN_CLAUDE_WORKSPACE=\"$(pwd)\" && cd \"${CLAUDE_PLUGIN_ROOT}\" && bun install --no-summary 2>/dev/null && exec bun server.ts"]
    }
  }
}
